# Core
fastapi
uvicorn[standard]
pydantic
python-multipart

# Google ADK
google-adk

# Google Generative AI (Gemini)
google-genai
python-dotenv

# MongoDB
pymongo
motor

# Google Cloud Storage (resume file storage)
google-cloud-storage

# MCP server
fastmcp

# DOCX and Excelgeneration
python-docx
xlsxwriter
pandas
openpyxl

# PDF reading (for resume extraction)
PyMuPDF                 # fitz — fast PDF text + image extraction
Pillow                  # PIL — embedded image extraction from DOCX resumes

# Auth — Entra ID SSO (no passwords; passlib/bcrypt removed with the old
# email+password login).
python-jose[cryptography]   # id_token signature verification against Entra JWKS
httpx                       # OIDC token exchange + Microsoft Graph calls
slowapi

# Observability
langfuse
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http 

email-validator
pydantic-settings